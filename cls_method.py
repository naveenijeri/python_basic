#instance , class , static method

#instance method - if we are using any instance variable then that method is called instance method
#class method - if we are using any any static or class variable then that method is called class method
#static method is called general utility methos in this methos we are not passing self, instance or static variable.
